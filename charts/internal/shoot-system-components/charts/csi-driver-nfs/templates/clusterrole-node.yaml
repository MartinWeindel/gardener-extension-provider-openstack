{{- if not .Values.pspDisabled }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "csi-driver-nfs-node.extensionsGroup" . }}:{{ include "csi-driver-nfs-node.name" . }}:csi-driver-nfs-node
rules:
  - apiGroups: ["policy", "extensions"]
    resourceNames: ["{{ include "csi-driver-nfs-node.extensionsGroup" . }}.{{ include "csi-driver-nfs-node.name" . }}.csi-driver-nfs-node"]
    resources: ["podsecuritypolicies"]
    verbs: ["use"]
{{- end }}